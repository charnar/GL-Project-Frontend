const state = {
  username: "",
  profilePicture: "",
};

const getters = {};

const actions = {};

const mutations = {
  setUsername(state, username) {
    state.username = username;
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
